// matrix by vector multiplication functions

void ZeroVector(double* v, size_t N);

void RandomVector(double* v, size_t N);

void CalcMatVecMul(const double* A, const double* u, double* v, size_t N);

